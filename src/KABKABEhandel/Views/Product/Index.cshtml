@{
    ViewData["Title"] = "Product Page";
}

<div class="row">
    <div class="col-md-3">
        <h2>Pencils</h2>
        @if (Model.IsActive)
        {
            <div class="item">
                <img src="@Model.ImageURL" alt="Penna" class="img-responsive" />
            </div>
            <h2>Model.Name</h2>
            <h2>Model.Description</h2>
            <h2>Model.Price</h2>

        }
        
    </div>
    <div class="col-md-3">
        <h2>Printers</h2>
        @if (Model.IsActive)
        {
            <div class="item">
                <img src="@Model.ImageURL" alt="Skrivare" class="img-responsive" />
            </div>
            <h2>Model.Name</h2>
            <h2>Model.Description</h2>
            <h2>Model.Price</h2>

        }
        
    </div>
    <div class="col-md-3">
        <h2>Papers</h2>
        @if (Model.IsActive)
        {
        <div class="item">
            <img src="@Model.ImageURL" alt="Papper" class="img-responsive" />
        </div>
        <h2>Model.Name</h2>
        <h2>Model.Description</h2>
        <h2>Model.Price</h2>

        }
    </div>
    <div class="col-md-3">
        <h2>Staplers</h2>
        @if (Model.IsActive)
        {
        <div class="item">
            <img src="@Model.ImageURL" alt="Haftapparat" class="img-responsive" />
            <h2>Model.Name</h2>
            <h2>Model.Description</h2>
            <h2>Model.Price</h2>

        </div>
        }
    </div>
</div>

